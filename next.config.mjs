/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["assets.aceternity.com", "media.istockphoto.com","images.unsplash.com","res.cloudinary.com"],  // Allow images from this domain
  },
};

export default nextConfig;
